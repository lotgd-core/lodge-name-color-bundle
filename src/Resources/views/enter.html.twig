{% trans_default_domain translation_domain %}

{{ 'section.default.subtitle'|trans(_context)|colorize }}

{{ 'section.default.say'|trans(_context)|colorize }}

{% if re_buy %}
    {{ 'section.default.re_buy.yes'|trans(_context)|colorize }}
{% else %}
    {{ 'section.default.re_buy.no'|trans(_context)|colorize }}
{% endif %}

{% if is_preview %}
    <div class="ui segment">
        {{ 'section.preview.paragraph'|trans(_context)|colorize }}
    </div>
{% endif %}

{% if is_name_colorize %}
    <div class="ui success message">
        {{ 'section.changed.paragraph'|trans(_context)|colorize }}
    </div>
{% endif %}

{{ 'section.default.buy'|trans(_context)|colorize }}

{{ 'section.default.current'|trans(_context) }}: {{ reg_name }},
{{ 'section.default.look'|trans(_context)|colorize }}

{{ form_start(form) }}
    {{ form_row(form.new_name) }}
    <div class="fields">
        {{ form_row(form.button_preview) }}
        {{ form_row(form.button_change) }}
    </div>
{{ form_end(form) }}

<div class="ui info message">
    {{ 'section.default.note'|trans(_context)|uncolorize|nl2br }}
</div>
